mobinorApp.controller('homeController', function ($scope, $location) {

    $scope.message = "Every One Come and See my First Routing Application";

    ////Method to route to the addemployee
    //$scope.Home = function () {
    //    $location.path("/Home");
    //}

    //$scope.Home = function () {
    //    if (window.location.href.endsWith('Home')) {
    //        $route.reload();
    //    };
    //};
    //$scope.Dailer = function () {
    //    if (window.location.href.endsWith('Dailer')) {
    //        $route.reload();
    //    };
    //};
});